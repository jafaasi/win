services:
  - type: web
    name: wingo-ai-backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "python backend/daemon_runner.py & uvicorn backend.server:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: DATABASE_URL
        sync: false
